# ViewModel for saved state

Use this document to create a `ViewModel` to store UI data where the data needs to be persisted across the activity lifecycle.

# Step 1. Read the documentation

1. Read the official Android documentation about [how to create a Saved state module for `ViewModel`](https://developer.android.com/topic/libraries/architecture/viewmodel/viewmodel-savedstate#savedstate-compose-state)